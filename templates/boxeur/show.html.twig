{% extends 'base.html.twig' %}

{% block title %}Boxeur{% endblock %}

{% block body %}
    <h1>Boxeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ boxeur.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ boxeur.imageName }}</td>
            </tr>
            <tr>
                <th>NomPrenom</th>
                <td>{{ boxeur.nomPrenom }}</td>
            </tr>
            <tr>
                <th>Information</th>
                <td>{{ boxeur.information }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_boxeur_index') }}">back to list</a>

    <a href="{{ path('app_boxeur_edit', {'id': boxeur.id}) }}">edit</a>

    {{ include('boxeur/_delete_form.html.twig') }}
{% endblock %}
