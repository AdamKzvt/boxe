<form method="post" action="{{ path('app_boxeur_delete', {'id': boxeur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boxeur.id) }}">
    <button class="btn">Delete</button>
</form>
